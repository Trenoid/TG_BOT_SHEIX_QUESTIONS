# Вопросы шейху — Telegram-бот

Бот для исламского проекта: пользователь задаёт вопрос шейху, выбирает тему и язык интерфейса, а шейх/администратор получает вопрос в отдельной панели, отвечает прямо в боте и закрывает обращение.

## Что есть в версии Sheikh Panel

- Отдельное меню для обычного пользователя.
- Отдельная панель для шейха/админа.
- У админа больше нет кнопки «Задать вопрос шейху».
- Шейх видит список вопросов: новые, отвеченные, закрытые.
- Шейх может открыть карточку вопроса, ответить и закрыть обращение.
- Поддержка нескольких шейхов/админов через `ADMIN_IDS`.
- 3 языка интерфейса для пользователей: русский, английский, арабский.
- Смена языка через кнопку и команду `/language`.
- Исламские категории вопросов:
  - Акыда / основы веры
  - Намаз, пост, закят, хадж
  - Фикх / бытовые вопросы
  - Семья и брак
  - Работа, бизнес и деньги
  - Коран и хадисы
  - Нравственность и поведение
  - Другое
- Поддержка текстов, фото, документов, видео и голосовых сообщений.
- Хранение истории в SQLite.

## Команды пользователя

```text
/start — открыть меню
/new — задать вопрос
/my — мои вопросы
/language — сменить язык
/help — помощь
```

## Команды шейха/админа

```text
/start — открыть панель шейха
/panel — открыть панель шейха
/answer 123 — ответить на вопрос #123
/close 123 — закрыть вопрос #123
/language — сменить язык панели
/help — помощь для шейха
/cancel — отменить ввод ответа
```

## Установка

### 1. Распакуйте архив

Перейдите в папку проекта:

```powershell
cd tg_support_bot_sheikh
```

### 2. Создайте виртуальное окружение

```powershell
python -m venv venv
```

### 3. Активируйте окружение

PowerShell может блокировать запуск скриптов. Самый простой вариант:

```powershell
cmd /k .\venv\Scripts\activate.bat
```

Или без активации можно запускать так:

```powershell
.\venv\Scripts\python.exe -m pip install -r requirements.txt
.\venv\Scripts\python.exe -m app.main
```

### 4. Установите зависимости

```powershell
pip install -r requirements.txt
```

### 5. Создайте `.env`

Скопируйте пример:

```powershell
copy .env.example .env
```

Заполните файл `.env`:

```env
BOT_TOKEN=токен_бота_от_BotFather
ADMIN_IDS=691367615
DATABASE_PATH=data/support_bot.db
```

Несколько админов можно указать через запятую:

```env
ADMIN_IDS=691367615,123456789,987654321
```

## Запуск

Правильная команда:

```powershell
python -m app.main
```

Не нужно запускать так:

```powershell
python -m app.main.py
```

## Как работает

### Пользователь

1. Открывает `/start`.
2. Выбирает язык.
3. Нажимает «Задать вопрос шейху».
4. Выбирает категорию.
5. Отправляет вопрос.
6. Получает ответ от шейха в этом же боте.

### Шейх/админ

1. Открывает `/start` или `/panel`.
2. Видит панель шейха.
3. Открывает новые вопросы.
4. Нажимает на нужный вопрос.
5. Нажимает «Ответить».
6. Пишет ответ текстом или отправляет медиа.
7. Ответ приходит пользователю.
8. При необходимости закрывает вопрос.

## Частые ошибки

### TelegramUnauthorizedError: Unauthorized

Проверьте `BOT_TOKEN` в `.env`. Это должен быть именно токен от BotFather.

### No module named tzdata

В этой версии `tzdata` уже добавлен в `requirements.txt`. Выполните:

```powershell
pip install -r requirements.txt
```

### PowerShell запрещает активацию venv

Запустите:

```powershell
cmd /k .\venv\Scripts\activate.bat
```

или используйте Python напрямую из окружения:

```powershell
.\venv\Scripts\python.exe -m app.main
```

## Обновление: скрытие карточки вопроса и история ответов

В этой версии для обычного пользователя скрыта кнопка «Открыть вопрос». Пользователь видит только краткий список своих вопросов и получает ответ шейха прямо в чат.

Для админов/шейхов добавлена история:

- кнопка «📜 История ответов» в панели шейха;
- кнопка «📜 История вопроса» в карточке каждого вопроса;
- в истории видно номер вопроса, дату создания, пользователя, тему, статус, кто и когда отвечал, текст ответа или тип вложения.

После загрузки новой версии на сервер выполните:

```bash
cd /home/tg_bot_QUESTSHEIHU/TG_BOT_SHEIH_QUESTIONS
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart sheikh-bot
sudo journalctl -u sheikh-bot -f
```

## Исправление пользовательского отклика

В этой версии пользовательские обработчики дополнительно защищены фильтром `NonAdminFilter`: обычные пользователи всегда попадают в пользовательское меню, а админы/шейхи — только в панель шейха. Это исправляет ситуацию, когда у админа всё работает, а обычный пользователь не получает ответ на `/start` или кнопки.

После обновления на сервере:

```bash
cd /home/tg_bot_QUESTSHEIHU/TG_BOT_SHEIH_QUESTIONS
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart sheikh-bot
sudo journalctl -u sheikh-bot -f
```

## Тесты

В проект добавлены базовые тесты:

- проверка парсинга `ADMIN_IDS`;
- проверка, что у пользователя нет кнопки открытия карточки вопроса;
- проверка фильтра, который разделяет админов и обычных пользователей;
- проверка истории ответов админов в базе данных.

Установить зависимости для тестов:

```bash
pip install -r requirements.txt -r requirements-dev.txt
```

Запустить тесты:

```bash
pytest -q
```

## Обновление: история ответов, пагинация и меню Telegram

В этой версии добавлено:

- В общей истории ответов админов/шейхов теперь отображается не только ответ, но и сам исходный вопрос пользователя.
- История ответов разбита на страницы, чтобы длинные списки не упирались в лимит Telegram и не превращались в слишком большое сообщение.
- Внизу возле поля ввода Telegram появляется стандартная кнопка меню команд. Для обычных пользователей доступны `/start`, `/new`, `/my`, `/language`, `/help`. Для админов дополнительно настраиваются команды панели: `/panel`, `/answer`, `/close`, `/cancel`.

После обновления кода на сервере выполните:

```bash
cd /home/tg_bot_QUESTSHEIHU/TG_BOT_SHEIH_QUESTIONS
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart sheikh-bot
sudo journalctl -u sheikh-bot -f
```

Для запуска тестов:

```bash
pip install -r requirements.txt -r requirements-dev.txt
pytest -q
```

## Обновление: полная история ответов для админов

В админской панели раздел **📜 История ответов** теперь работает в два уровня:

1. В общем списке показываются только краткие превью вопроса и ответа, чтобы сообщение не обрезалось Telegram.
2. Под списком есть кнопки **📄 Полный текст: вопрос #... / ответ #...**.
3. По кнопке открывается полная карточка с:
   - номером вопроса;
   - темой;
   - статусом;
   - языком пользователя;
   - кем задан вопрос;
   - временем вопроса;
   - полным текстом вопроса;
   - кем отвечено;
   - временем ответа;
   - полным текстом ответа.

Такую полную карточку удобно пересылать в группу. Если карточка слишком длинная, бот автоматически отправит её несколькими частями.


## Обновление: голосовые сообщения в истории

В истории ответов голосовые сообщения больше не отображаются как `[ContentType.VOICE]`.
Бот показывает понятную пометку `🎙 Голосовое сообщение`, а в полной карточке ответа появляются кнопки:

- `📎 Отправить оригинал вопроса` — если вопрос был голосовым/файлом/фото/видео;
- `🎙/📎 Отправить оригинал ответа` — если ответ шейха был голосовым/файлом/фото/видео.

Админ может нажать кнопку, получить оригинальное голосовое отдельным сообщением и переслать его в группу.
